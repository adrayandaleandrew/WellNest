# E2E test credentials — copy this file to .env.test and fill in real values.
# .env.test is gitignored; never commit credentials.
PLAYWRIGHT_ADMIN_EMAIL=your-admin@example.com
PLAYWRIGHT_ADMIN_PASSWORD=YourAdminPassword1
